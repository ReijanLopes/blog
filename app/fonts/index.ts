import { Inter, Roboto_Mono, Archivo_Black } from "next/font/google";

export const roboto_mono = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
});

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const archivo_black = Archivo_Black({
  weight: "400",
  subsets: ["latin"],
  display: "auto",
});
